"use client"

/**
 * Untitled **collection** table: RAC `Table` + sorting/selection/columns (see DS stories).
 * For classic HTML `<table>` + `TableRow` / `TableCell`, use `@/components/ui/table`.
 */
export { Table, TableCard } from "@/components/application/table/table"
